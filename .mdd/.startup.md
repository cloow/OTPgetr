---
mdd_version: 11
---
## Project Snapshot
Generated: 2026-07-19 | Branch: main

## Stack
Language: python | Auth: Google OAuth (gmail.readonly) | Host: local Windows desktop

## Features Documented
- 01-otpgetr (draft) [otp, gmail, gmail-api, oauth, clipboard, ocr, anthropic, hotkeys, windows, spam-review]

## Ops Runbooks
(none yet — run /mdd ops <description> to create one)

## Last Audit
(no audit run yet — run /mdd audit to generate findings)

## Rules Summary
Read CLAUDE.md for the full rulebook. Key rules:
- Never commit .env, credentials.json, token.json, api_key.txt
- Anthropic key comes from the ANTHROPIC_API_KEY env var (User scope) — no secret in source
- Spam-review gate is fail-open and removable (CLAUDE_REVIEW_ENABLED)
- Always branch — never commit to main

---

## Notes
- [2026-07-19] Migrated this doc from the shared root .mdd into OTPgetr's own repo .mdd (standard per-repo MDD). Drift detection now works via this repo's git history.
- [2026-07-19] Removed OTPgetr_personal.py (hardcoded key). Key now via ANTHROPIC_API_KEY env var (User scope, set to a 108-char sk-ant key). Added a "use env var" checkbox to the first-run setup popup; env is read first so it suppresses the prompt and works like a pasted key.
- [2026-07-19] Added Ctrl+Alt+K runtime API-key window (add/change/turn off, no restart; reset_claude_client() re-reads the key) + a rate-limited passive toast (KEY_NUDGE_COOLDOWN_MIN=30) that fires only when an image OTP arrives with no key set. Uses the existing no-focus-steal popup so it won't interrupt fullscreen. Shared _key_form_widgets() between first-run and runtime windows.
